import { IsLoginDirective } from './is-login.directive';

describe('IsLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLoginDirective();
    expect(directive).toBeTruthy();
  });
});
